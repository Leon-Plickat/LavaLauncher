# Todo
* Additional 'return' command that causes LavaLauncher to exit with a given number,
  which might be interesting for scripting
* Different background when hovering over icon
* Documentation: Change "bar size" to "icon size"
* Check if scale is correctly implemented
